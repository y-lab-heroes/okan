export declare type AsyncTask<T> = () => Promise<T>;
export interface SerialExecutor {
    readonly isActive: boolean;
    size(): number;
    execute<T>(task: AsyncTask<T>, timeout?: number): Promise<T>;
}
export declare function createSerialExecutor(): SerialExecutor;
export declare class SerialExecutorTimeoutError extends Error {
    constructor(key: string, timeout: number);
}
